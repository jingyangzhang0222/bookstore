<div id="books-show">
	<h2><%= @book.title %></h2>
	<div class="books-show-left">
		<%= image_tag(@book.coverpath) %>
	</div>
	<br>
	<div class="books-show-right">
		<h3>Books Info:</h3>
		<ul>
			<li><string>Category: </string> <%= @book.category.name %></li>
			<li><string>Author: </string> <%= @book.author.first_name %> <%= @book.author.last_name %></li>
			<li><string>Publisher: </string> <%= @book.publisher.name %></li>
			<li><string>Year: </string> <%= @book.year %></li>
			<li><string>ISBN: </string> <%= @book.isbn %></li>
			<li><string>Format: </string> <%= @book.format %></li>
			<li><string>Pages: </string> <%= @book.pages %></li>
		</ul>
		<%=link_to "Buy For "+@book.price, @book.buy, class:'btn btn-success',target:'_blank' %>
	</div>
	<div class="clearfix"></div>
	<br>
	<br>
	<h4>Excerpt</h4>
	<p><%= @book.excerpt %></p>

	<h3>Admin Functions</h3>
	<%= link_to 'Edit', edit_book_path(@book.id) %> | <%= link_to 'Delete', book_path(@book), method: :delete, data: {confirm: 'Are you sure?'} %>
</div>